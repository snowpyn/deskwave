# Contributing to DeskWave

Use focused commits, preserve the protocol's backward compatibility, and add
tests for behavior changes. Never commit credentials, pairing tokens, Wi-Fi
passwords, generated firmware, virtual environments, or local runtime state.

Before opening a change, run the firmware build and the host formatting, lint,
type, and test commands documented in the README.
